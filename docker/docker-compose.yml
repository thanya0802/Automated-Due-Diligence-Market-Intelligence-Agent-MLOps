version: '3.8'

services:
  postgres:
    image: postgres:13
    environment:
      - POSTGRES_USER=airflow
      - POSTGRES_PASSWORD=airflow
      - POSTGRES_DB=airflow
    volumes:
      - postgres-db-volume:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "airflow"]
      interval: 5s
      retries: 5
    restart: always

  airflow-init:
    build:
      context: ..
      dockerfile: docker/Dockerfile.airflow
    depends_on:
      - postgres
    env_file:
      - ../.env
    environment:
      - AIRFLOW__CORE__EXECUTOR=LocalExecutor
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres/airflow
      - AIRFLOW__CORE__LOAD_EXAMPLES=False
      - AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION=False
      - _AIRFLOW_DB_UPGRADE=true
      - _AIRFLOW_WWW_USER_CREATE=true
      - _AIRFLOW_WWW_USER_USERNAME=admin
      - _AIRFLOW_WWW_USER_PASSWORD=admin
      - AIRFLOW__SMTP__SMTP_HOST=${SMTP_SERVER}
      - AIRFLOW__SMTP__SMTP_PORT=${SMTP_PORT}
      - AIRFLOW__SMTP__SMTP_USER=${ALERT_EMAIL_SENDER}
      - AIRFLOW__SMTP__SMTP_PASSWORD=${ALERT_EMAIL_PASSWORD}
      - AIRFLOW__SMTP__SMTP_MAIL_FROM=${ALERT_EMAIL_SENDER}
      - AIRFLOW__SMTP__SMTP_STARTTLS=True
      - AIRFLOW__SMTP__SMTP_SSL=False
    volumes:
      - ../dags:/opt/airflow/dags
      - ../src:/opt/airflow/src
      - ../data:/opt/airflow/data
      - ../config:/opt/airflow/config
      - ../logs:/opt/airflow/logs
    entrypoint: /bin/bash
    command:
      - -c
      - |
        airflow db init
        airflow users create --username admin --password admin --firstname Admin --lastname User --role Admin --email admin@example.com || true
        airflow variables set target_company "Apple Inc."
        airflow variables set news_api_key "$$NEWS_API_KEY" || true
        airflow variables set sec_api_key "$$SEC_API_KEY" || true

  airflow-webserver:
    build:
      context: ..
      dockerfile: docker/Dockerfile.airflow
    depends_on:
      - postgres
      - airflow-init
    env_file:
      - ../.env
    environment:
      - AIRFLOW__CORE__EXECUTOR=LocalExecutor
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres/airflow
      - AIRFLOW__CORE__LOAD_EXAMPLES=False
      - AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION=False
      - AIRFLOW__SMTP__SMTP_HOST=${SMTP_SERVER}
      - AIRFLOW__SMTP__SMTP_PORT=${SMTP_PORT}
      - AIRFLOW__SMTP__SMTP_USER=${ALERT_EMAIL_SENDER}
      - AIRFLOW__SMTP__SMTP_PASSWORD=${ALERT_EMAIL_PASSWORD}
      - AIRFLOW__SMTP__SMTP_MAIL_FROM=${ALERT_EMAIL_SENDER}
      - AIRFLOW__SMTP__SMTP_STARTTLS=True
      - AIRFLOW__SMTP__SMTP_SSL=False
    volumes:
      - ../dags:/opt/airflow/dags
      - ../src:/opt/airflow/src
      - ../data:/opt/airflow/data
      - ../config:/opt/airflow/config
      - ../logs:/opt/airflow/logs
    ports:
      - "8080:8080"
    command: webserver
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8080/health"]
      interval: 10s
      timeout: 10s
      retries: 5
    restart: always

  airflow-scheduler:
    build:
      context: ..
      dockerfile: docker/Dockerfile.airflow
    depends_on:
      - postgres
      - airflow-init
    env_file:
      - ../.env
    environment:
      - AIRFLOW__CORE__EXECUTOR=LocalExecutor
      - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres/airflow
      - AIRFLOW__CORE__LOAD_EXAMPLES=False
      - AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION=False
      - AIRFLOW__SMTP__SMTP_HOST=${SMTP_SERVER}
      - AIRFLOW__SMTP__SMTP_PORT=${SMTP_PORT}
      - AIRFLOW__SMTP__SMTP_USER=${ALERT_EMAIL_SENDER}
      - AIRFLOW__SMTP__SMTP_PASSWORD=${ALERT_EMAIL_PASSWORD}
      - AIRFLOW__SMTP__SMTP_MAIL_FROM=${ALERT_EMAIL_SENDER}
      - AIRFLOW__SMTP__SMTP_STARTTLS=True
      - AIRFLOW__SMTP__SMTP_SSL=False
    volumes:
      - ../dags:/opt/airflow/dags
      - ../src:/opt/airflow/src
      - ../data:/opt/airflow/data
      - ../config:/opt/airflow/config
      - ../logs:/opt/airflow/logs
    command: scheduler
    restart: always

volumes:
  postgres-db-volume:
